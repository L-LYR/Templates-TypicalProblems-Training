#include <bits/stdc++.h>
using namespace std;

auto main() -> int {
    auto t = 0;
    cin >> t;
    while (t-- > 0) {
        auto n = string();
        cin >> n;
        cout << n.length() << endl;
    }
    return 0;
}