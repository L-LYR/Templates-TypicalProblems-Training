#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>

using namespace std;
using ll = long long;
const int inf = 0x3f3f3f3f;
const int maxn = 405;

int a[maxn];
ll dp[maxn][maxn];

int main(void){
    int n;
    cin >> n;
    for (int i = 0; i < n; ++i) cin >> a[i];
    memset(dp, 0x3f, sizeof(dp));

    for (int i = 0; i < n; ++i) {
        for (int j = 0; j < n; ++ i) {
            
        }
    }

    return 0;
}